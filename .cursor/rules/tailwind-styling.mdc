---
description: 
globs: 
alwaysApply: false
---
---
description: Suggest clean and responsive Tailwind CSS styling for any component, focusing on layout, spacing, responsiveness, and interaction states.
globs: ["src/components/**/*.tsx"]
alwaysApply: false
---

# 🎨 Tailwind Styling Prompt

## Purpose

Apply clean, consistent Tailwind CSS styles to components. Focus on spacing, layout, color, and responsive behavior.

## Guidelines

- Use `flex`, `grid`, or `space-*` to control layout
- Apply responsive prefixes (`sm:`, `md:`) for breakpoints
- Style hover/focus/disabled states
- Keep className values readable — avoid long single lines

## Example Prompt

> “Style the component with Tailwind: make it centered, add spacing between elements, responsive padding, rounded corners, and subtle hover effect on button.”

## Example Output

```tsx
<div className="flex flex-col items-center justify-center px-4 py-6 sm:px-8 sm:py-10 space-y-4 bg-white rounded-xl shadow-md">
  <h2 className="text-xl font-semibold text-gray-800">Login</h2>

  <form className="w-full max-w-sm space-y-4">
    <input
      className="w-full border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
      placeholder="Email"
    />
    <input
      className="w-full border border-gray-300 rounded-md p-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
      placeholder="Password"
      type="password"
    />
    <button
      className="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition-colors"
    >
      Submit
    </button>
  </form>
</div>
